@model IEnumerable<BookShopModel.Model.Theme>
@{
    ViewBag.Title = "Index";
}
<h2>
    Themes</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table border="1px">
    <tr>
        <th>
            ThemeName
        </th>
        <th>
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ThemeName)
            </td>
            <td>
                @using (Html.BeginForm("Delete", "Theme", new { id = item.Id }))
                {
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) @:|
                    <input type="submit" value="Delete" />
                }
            </td>
        </tr>
    }
</table>
